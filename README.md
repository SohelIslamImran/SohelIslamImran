# Sohel Islam Imran — Folio

Personal site: an optical light table. Built with **TanStack Start**, React 19, and **Bun**.

```bash
bun install
bun run dev
```

Public pages: `/` `/work` `/story` `/field-notes` `/resume` `/links`. Studio is owner-gated.

Accent **gels** (bottom-left) swap the primary live — Sky, Azure, and the rest — and stick in the browser.
